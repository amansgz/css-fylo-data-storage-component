const innerBar = document.getElementById('storage-inner-bar');

innerBar.style.width = `${100/(1000 / 815)}%`;
innerBar.style.backgroundImage = 'linear-gradient(hsl(6, 100%, 80%), hsl(335, 100%, 65%))';